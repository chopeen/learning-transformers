name: transformers-qa
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7.*
  - beautifulsoup4
  - lxml
  - pip
  - tabulate
  - pip:
    - scikit-learn
    - tensorflow
    - transformers
    
    # Torch instead of TF
    # - torch
    # - torchvision
